# data/

Place your sample PDF textbooks here for local testing.

These files are excluded from git (see `.gitignore`).
When running via `docker-compose`, this folder is mounted into the
container at `/app/data`.
